Next steps to do in this order:
* Create own Url class
+ Bugreport to Living URL Standard: add backspace to list of control chars
* Bugreport to gtest: EXPECT_THAT THROW shouldn't call two times if no match
* Install doxygen
